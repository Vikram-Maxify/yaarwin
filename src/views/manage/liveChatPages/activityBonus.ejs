<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Activity Bonus</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700&display=fallback">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" crossorigin="anonymous" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/dist/css/adminlte.min.css">
  <link rel="stylesheet" href="/css/admin.css">
  <!-- SweetAlert2 CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">

  <style>
    body {
      background-color: #f4f6f9;
      color: #343a40;
      font-family: 'Source Sans Pro', sans-serif;
    }
    .card {
      border-radius: 12px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
      background: #ffffff;
    }
    .table-responsive {
      overflow-x: auto;
    }
    .table th {
      background-color: #00807c;
      color: white;
    }
    .table-striped tbody tr:nth-of-type(odd) {
      background-color: #DFF5F5;
    }
    .btn-primary {
      background-color: #00807c;
      border-color: #00807c;
    }
    .btn-primary:hover {
      background-color: #065e5b;
      border-color: #065e5b;
    }
    .table img {
      width: 80px;
      height: 80px;
      object-fit: cover;
      cursor: pointer;
      border-radius: 8px;
      transition: transform 0.3s;
    }
    .table img:hover {
      transform: scale(1.05);
    }
    .action-buttons {
      display: flex;
      gap: 8px;
      justify-content: center;
    }
    .btn-sm {
      padding: 0.25rem 0.5rem;
      font-size: 0.875rem;
    }
    .modal-img-container {
      max-height: 70vh;
      overflow: auto;
    }
    .modal-img-container img {
      max-width: 100%;
      height: auto;
    }
    .no-image {
      background-color: #f8f9fa;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
      color: #6c757d;
    }
  </style>
</head>

<body class="hold-transition sidebar-mini">
  <div class="wrapper">
    <%- include('../nav') %>
    <div class="content-wrapper">
      <section class="content-header">
        <div class="container-fluid">
          <div class="row mb-2">
            <div class="col-sm-6">
              <h1 class="text-dark">Activity Bonus</h1>
            </div>
          </div>
        </div>
      </section>
      
      <section class="content">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Bonus Claim Details</h3>
          </div>
          <div class="card-body p-0 table-responsive">
            <table class="table table-striped projects">
              <thead>
                <tr>
                  <th class="text-center">#</th>
                  <th class="text-center">Bonus Claim Explanation</th>
                  <th class="text-center">Tiranga ID</th>
                  <th class="text-center">Winning Record Screenshot</th>
                  <th class="text-center">Action</th>
                </tr>
              </thead>
              <tbody id="bonusTable">
                <tr>
                  <td colspan="5" class="text-center">Loading bonus claims...</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </section>
    </div>
  </div>

  <!-- Image Popup Modal -->
  <div class="modal fade" id="imageModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Screenshot Preview</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body text-center modal-img-container">
          <img id="modalImage" src="" class="img-fluid" alt="Winning Record Screenshot">
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/dist/js/adminlte.min.js"></script>
  <script src="/js/admin/admin.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

  <script>
    $(document).ready(function () {
      // Load bonus details
      function loadBonusDetails() {
        $.ajax({
          type: "GET",
          url: "/api/webapi/bonusdetails",
          dataType: "json",
          success: function (response) {
            let html = "";
            const data = response.data || response; // Handle different response structures
            
            if (!Array.isArray(data) || data.length === 0) {
              html = '<tr><td colspan="5" class="text-center">No bonus claims found.</td></tr>';
            } else {
              data.forEach((item, index) => {
                const screenshotImg = item.winningRecord ? item.winningRecord : '/dist/img/no-image.png';
                
                html += `
                  <tr class="text-center">
                    <td>${index + 1}</td>
                    <td>${item.winningRecordExplanation || "No explanation provided"}</td>
                    <td>${item.TirangaID || "N/A"}</td>
                    <td>
                      <div class="${!item.winningRecord ? 'no-image' : ''}" style="width:80px;height:80px;margin:0 auto">
                        ${item.winningRecord ? 
                          `<img src="${screenshotImg}" class="preview-img" onclick="showImage('${screenshotImg}')">` : 
                          'No Image'}
                      </div>
                    </td>
                    <td class="action-buttons">
                      <button class="btn btn-success btn-sm approve-btn" data-id="${item.id}">
                        <i class="fas fa-check"></i> Approve
                      </button>
                      <button class="btn btn-danger btn-sm delete-btn" data-id="${item.id}">
                        <i class="fas fa-trash"></i> Delete
                      </button>
                    </td>
                  </tr>`;
              });
            }
            
            $("#bonusTable").html(html);
          },
          error: function(xhr, status, error) {
            console.error("Error loading bonus details:", error);
            $("#bonusTable").html('<tr><td colspan="5" class="text-center text-danger">Failed to load data. Please try again.</td></tr>');
          }
        });
      }

      // Show image in modal
      window.showImage = function(imgSrc) {
        $("#modalImage").attr("src", imgSrc);
        $("#imageModal").modal("show");
      };

      // Approve Action
$(document).on("click", ".approve-btn", function () {
  const bonusId = $(this).data("id");

  Swal.fire({
    title: "Approve Bonus Claim?",
    text: "Are you sure you want to approve this bonus claim?",
    icon: "question",
    showCancelButton: true,
    confirmButtonColor: "#28a745",
    cancelButtonColor: "#d33",
    confirmButtonText: "Yes, Approve!",
    cancelButtonText: "Cancel",
    reverseButtons: true
  }).then((result) => {
    if (result.isConfirmed) {
      $.ajax({
        type: "POST",
        url: `/api/webapi/bonusstatus/accepted/${bonusId}`,  // Correct route for approving bonus claim
        dataType: "json",
        success: function (response) {
          Swal.fire({
            title: "Approved!",
            text: "The bonus claim has been approved.",
            icon: "success",
            confirmButtonColor: "#28a745"
          });
          loadBonusDetails();  // Refresh the bonus details list
        },
        error: function (xhr, status, error) {
          Swal.fire({
            title: "Error!",
            text: "Failed to approve bonus claim. Please try again.",
            icon: "error",
            confirmButtonColor: "#d33"
          });
        }
      });
    }
  });
});

// Delete Action
$(document).on("click", ".delete-btn", function () {
  const bonusId = $(this).data("id");

  Swal.fire({
    title: "Delete Bonus Claim?",
    text: "This will permanently delete the bonus claim record.",
    icon: "warning",
    showCancelButton: true,
    confirmButtonColor: "#d33",
    cancelButtonColor: "#6c757d",
    confirmButtonText: "Yes, Delete!",
    cancelButtonText: "Cancel",
    reverseButtons: true
  }).then((result) => {
    if (result.isConfirmed) {
      $.ajax({
        type: "POST",
        url: `/api/webapi/bonusstatus/rejected/${bonusId}`,  // Correct route for deleting bonus claim
        dataType: "json",
        success: function (response) {
          Swal.fire({
            title: "Deleted!",
            text: "The bonus claim has been removed.",
            icon: "success",
            confirmButtonColor: "#28a745"
          });
          loadBonusDetails();  // Refresh the bonus details list
        },
        error: function (xhr, status, error) {
          Swal.fire({
            title: "Error!",
            text: "Failed to delete bonus claim. Please try again.",
            icon: "error",
            confirmButtonColor: "#d33"
          });
        }
      });
    }
  });
});


      // Initial load
      loadBonusDetails();
    });
  </script>
</body>
</html>